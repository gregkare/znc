---
driver_plugin: vagrant

driver_config:
  require_chef_omnibus: 11.10.4

platforms:
  - name: ubuntu-13.10
  - name: ubuntu-12.10
    run_list:
      - recipe[apt]
  - name: ubuntu-12.04
    run_list:
      - recipe[apt]
  - name: centos-6.4
    run_list:
      recipe[yum-epel]

suites:
  - name: default
    run_list:
      - recipe[znc]
    attributes:
      znc:
        users:
          - id: john
            comment: John Connor
            znc:
              pass: hunter2
              ident: hunter@example.com
              alt_nick: dude
              server: chat.freenode.net +7000
